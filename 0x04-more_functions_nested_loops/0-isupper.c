#include "main.h"

/**
 * _isupper - Check if a letter is upper
 * @a: The number to be checked
 * Return: 1 for upper letter or 0 for any elese
 */

int _isupper(int a)

{

	if (a >= 65 && a <= 98)
	{
	return (1);
	}
	return (0);
}
